# Copyright 2022 The Fuchsia Authors. All rights reserved.
# Use of this source code is governed by a BSD-style license that can be
# found in the LICENSE file.

group("tests") {
  # NOT: Do not add //build/bazel/tests/build_action to
  # the dependency list of this group, as this slows down
  # all incremental builds considerably due to each
  # GN > Bazel invocation taking at least a second on a
  # powerful workstation.
  #
  # Instead, it is possible to add //build/bazel/tests/build_action
  # to the host_test_labels list in args.gn to enable these.
}
